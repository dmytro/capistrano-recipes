<% hosts.each do |location| %>GRANT ALL ON <%= database['database'] %>.* TO '<%= database['user'] %>'@'<%= location %>' IDENTIFIED BY '<%= database['password'] %>';
<% end -%>

CREATE DATABASE IF NOT EXISTS <%= database['database'] %>;

FLUSH PRIVILEGES;